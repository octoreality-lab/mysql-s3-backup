DB_HOST = localhost
DB_PORT = 3306
DB_USER = root
DB_PASSWORD = azerty
DB_NAME = mydatabase

S3_ENDPOINT = myendpoint
S3_REGION = myregion
S3_BUCKET_NAME = mybucket
S3_ACCESS_KEY = myaccesskey
S3_SECRET_KEY = mysecretkey

BACKUP_RETENTION_DAYS = 7

# Local log file (relative paths are resolved next to backup-script.py)
LOG_FILE = backup.log

# S3 object key prefix for backups (use trailing slash for a folder)
S3_PREFIX = backups/

# Optional: where mysqldump writes before zip (default: system temp)
# TEMP_DIR = /tmp

# Optional extra mysqldump CLI args (space-separated)
# MYSQLDUMP_EXTRA_ARGS = --quick --lock-tables=false